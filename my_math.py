from math import *

number = -5
number_float = 5.3

# rounds number up despite the decimal
print(ceil(number_float))

# rounds number down despite the decimal
print(floor(number_float))

# returns the absolute value of number, but it turns into a float
number = fabs(number)
print(number)

# prints the factorial of the number
print(factorial(number))
